version: "2" # required to adjust maintainability checks
checks:
  argument-count:
    config:
      threshold: 4
  complex-logic:
    config:
      threshold: 4
  file-lines:
    config:
      threshold: 250
  method-complexity:
    config:
      threshold: 5
  method-count:
    config:
      threshold: 20
  method-lines:
    config:
      threshold: 25
  nested-control-flow:
    config:
      threshold: 4
  return-statements:
    config:
      threshold: 4
  identical-code:
    enabled: false
  similar-code:
    enabled: false
plugins:
  duplication:
    enabled: true
    config:
      languages:
        typescript:
          mass_threshold: 20
exclude_patterns:
  - "bin/"
  - "**/node_modules/"
  - "**/spec/"
  - "**/test/"
  - "**/tests/"
  - "**/vendor/"
  - "**/migrations/"
  - "**/*.spec.ts"
  - "**/*.d.ts"
