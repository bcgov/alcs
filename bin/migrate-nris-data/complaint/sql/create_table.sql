create table if not exists nris.complaint (
  record_id varchar primary key,
  record_type varchar,
  status varchar,
  "date" timestamptz,
  region varchar,
  zone varchar,
  local_government varchar,
  c_e_officer varchar,
  related_records varchar,
  type varchar,
  reason varchar,
  primary_location_description varchar,
  latitude numeric(15, 5),
  longitude numeric(15, 5),
  primary_location_pid varchar,
  activity varchar,
  inspection_property_owner varchar,
  submission_property_owner varchar,
  area numeric(15, 5),
  percent_of_alr numeric(15, 5),
  application_history_prior_c_e_issues varchar,
  submitter varchar,
  privacy_requested varchar,
  submitter_email varchar,
  submitter_phone varchar,
  affiliation varchar,
  additional_contact_info varchar,
  client_contact varchar,
  client_address varchar,
  client_phone varchar,
  client_fax varchar,
  client_email varchar,
  action_taken varchar,
  date_action_taken timestamptz,
  priority varchar,
  other_agency varchar,
  assigned_to varchar,
  complaint_referral_processed_date timestamptz,
  in_attendance varchar,
  est_incident_date timestamptz,
  section varchar,
  alledged_non_compliance varchar,
  condition_title varchar,
  condition_description varchar,
  internal_notes varchar,
  description_and_comments varchar,
  compliance_status varchar,
  c_e_actions varchar,
  follow_up_date timestamptz,
  foippa_type varchar,
  ownership_type varchar,
  cleaned_local_government varchar,
  street_address varchar
)
