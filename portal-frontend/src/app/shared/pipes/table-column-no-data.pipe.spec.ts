import { TableColumnNoDataPipe } from './table-column-no-data.pipe';

describe('TableColumnNoDataPipe', () => {
  it('create an instance', () => {
    const pipe = new TableColumnNoDataPipe();
    expect(pipe).toBeTruthy();
  });
});
